allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    group = 'com.example'
    version = '1.0.0'

    dependencies {
        // Common dependencies can be added here
    }
}
